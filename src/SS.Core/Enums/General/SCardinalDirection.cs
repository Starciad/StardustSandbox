namespace StardustSandbox.Core.Enums.General
{
    public enum SCardinalDirection : byte
    {
        // (.)
        Center = 0,
        
        // (↑)
        North = 1,

        // (↗)
        Northeast = 2,

        // (→)
        East = 3,

        // (↘)
        Southeast = 4,

        // (↓)
        South = 5,

        // (↙)
        Southwest = 6,

        // (←)
        West = 7,

        // (↖)
        Northwest = 8,
    }
}
